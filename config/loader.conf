# Enable ZFS support
zfs_load="YES"

# Load the ZFS boot module
vfs.root.mountfrom="zfs:root"

# Enable swap on ZFS
zfs_swap="YES"

# Set the console to use for boot messages
console="comconsole,vidconsole"

# Enable verbose boot messages
verbose_loading="YES"
